trigger:
- main

pool:
  name: Default

steps:
- task: UseDotNet@2
  inputs:
    packageType: 'sdk'
    version: '8.x'

- script: dotnet restore
  displayName: 'Restore'

- script: dotnet build --configuration Release
  displayName: 'Build'

- script: dotnet pack --configuration Release --output $(Build.ArtifactStagingDirectory)
  displayName: 'Pack NuGet'

- task: NuGetAuthenticate@1
  displayName: 'Authenticate NuGet'

- script: dotnet nuget push "$(Build.ArtifactStagingDirectory)\*.nupkg" --source "https://pkgs.dev.azure.com/jtuazon8/Lab4-StringExtensions/_packaging/lab4feed/nuget/v3/index.json" --api-key AzureDevOps --skip-duplicate
  displayName: 'Push package'
